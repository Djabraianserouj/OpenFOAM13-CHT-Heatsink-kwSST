functions
{
    #includeFunc residuals              ( region = fluid, p_rgh, U, h, k, omega )
    #includeFunc residuals              ( region = solid, e )
    #includeFunc yPlus                  ( region = fluid )
    #includeFunc probes                 ( region = fluid )
    #includeFunc patchAverage_inlet     ( region = fluid )
    #includeFunc patchAverage_outlet    ( region = fluid )
    #includeFunc patchAverage_heater    ( region = solid )
}